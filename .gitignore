# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# Environment variables
.env
.env.*
!.env.example

# If you add additional committed env templates, update the allowlist above (e.g. `!.env.<name>.example`).
# Project policy: only allowlist specific template files; never use broad patterns (like `!.env*` or `!.env.*`) that could include real secrets.
# Example allowlist entries:
#   !.env.example              # current default template
#   !.env.local.example
#   !.env.prod.example
# Template env files must only contain placeholder values; never commit real secrets.

# Pytest / coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# mypy
.mypy_cache/
