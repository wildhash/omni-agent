# WARNING: This file is for LOCAL DEVELOPMENT ONLY.
# - Do NOT commit real secrets.
# - Do NOT enable OMNI_AGENT_ALLOW_INSECURE_NOAUTH, OMNI_AGENT_ENABLE_CODE_EXEC,
#   or OMNI_AGENT_ENABLE_DOCKER_BUILD in any shared or remotely accessible environment
#   (including staging, CI, or production).

# Backend API authentication (must be a strong, unguessable secret if exposed beyond localhost)
# NOTE: Replace this with a long, random value before binding to 0.0.0.0 or exposing the API to others.
OMNI_AGENT_API_KEY=CHANGE_ME__USE_A_STRONG_RANDOM_VALUE

# WARNING: OMNI_AGENT_ALLOW_INSECURE_NOAUTH disables all API authentication.
# When set, OMNI_AGENT_API_KEY is ignored.
# Never set this outside of isolated local development on your own machine.
# Do NOT use this if the server is bound to 0.0.0.0 or reachable by others.
# Do NOT use this in Docker or any non-localhost binding.
# OMNI_AGENT_ALLOW_INSECURE_NOAUTH=1

# Mistral AI (for code generation, self-healing, and agent auto-generation)
# Required for MistralClient, AgentGenerator, and SelfHealer features.
MISTRAL_API_KEY=your_mistral_api_key_here

# GitHub (for IssueAgent/ReleaseAgent)
# Use a least-privileged token for your use case.
GITHUB_TOKEN=your_personal_access_token

# Docker Hub (optional; typically only needed if you personally push images to Docker Hub)
# CI deploy uses GitHub Container Registry (GHCR) by default, so these are not required for local development.
DOCKER_USERNAME=your_username
DOCKER_PASSWORD=your_password

# Weaviate
# HTTP is intended for local development; production setups typically use auth (see docker-compose.prod.yml).
WEAVIATE_URL=http://localhost:8080

# Optional for basic local development. Required when using docker-compose.prod.yml.
WEAVIATE_API_KEY=
WEAVIATE_API_USER=omni-agent

# Dangerous capabilities (LOCAL DEV ONLY - can enable arbitrary code exec and Docker builds)
# Never include these variables in staging/production env files, or any environment exposed over a network.
# Combining these with OMNI_AGENT_ALLOW_INSECURE_NOAUTH is equivalent to unauthenticated remote code execution.
# Do NOT reuse your local `.env` in deployment configs.
# OMNI_AGENT_ENABLE_CODE_EXEC=
# OMNI_AGENT_ENABLE_DOCKER_BUILD=

# Self-heal + generated agents (LOCAL DEV ONLY)
# - OMNI_AGENT_ENABLE_SELF_HEAL_APPLY allows SelfHealer to write files under `omni_agent/` and `tests/`.
# - OMNI_AGENT_ENABLE_GENERATED_AGENTS allows importing/executing generated agent modules.
# OMNI_AGENT_ENABLE_SELF_HEAL_APPLY=
# OMNI_AGENT_ENABLE_GENERATED_AGENTS=
